{
  "family": "agent-workflow",
  "schema": 1,
  "name": "fixture-mode-catalog-bad-shape",
  "kind": "execution-backend",
  "version": "1.0.0",
  "provides": ["review"],
  "roles": {
    "review": { "cmd": "fixture-review", "source": "bin/run.sh", "modes": ["code"], "output": "advisory" }
  },
  "modeCatalog": {
    "review.code": { "kind": "primary", "role": "review", "purpose": "an object keyed by mode — not the declared array shape" }
  },
  "cost": "subscription"
}
