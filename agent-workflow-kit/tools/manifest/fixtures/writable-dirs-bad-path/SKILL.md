---
name: fixture-writable-dirs-bad-path
description: Glob, trailing-slash and traversal writableDirs defaults must fail validation.
metadata:
  version: '1.0.0'
---

# fixture-writable-dirs-bad-path

Test fixture — a `writableDirs` default carrying glob characters, a trailing slash, or `..` traversal is malformed.
