---
name: fixture-network-hosts-empty
description: An empty networkHosts array must fail validation.
metadata:
  version: '1.0.0'
---

# fixture-network-hosts-empty

Test fixture — an EMPTY `networkHosts` array is malformed (declare hosts or omit the key).
