---
name: fixture-writable-dirs-empty
description: An empty writableDirs array must fail validation.
metadata:
  version: '1.0.0'
---

# fixture-writable-dirs-empty

Test fixture — an EMPTY `writableDirs` array is malformed (declare entries or omit the key).
