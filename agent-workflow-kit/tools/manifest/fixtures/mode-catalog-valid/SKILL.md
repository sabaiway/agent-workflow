---
name: fixture-mode-catalog-valid
description: A valid modeCatalog manifest fixture for validate.test.mjs.
metadata:
  version: '1.0.0'
---

# fixture-mode-catalog-valid

Test fixture — the LITERAL binding shape of a well-formed `modeCatalog` block: every entry kind
(primary / continuation / env-hook), contract-backed plural `invocationRefs[]`, a contract-free
literal `descriptor`, typed `operands[]`, structured `guardrails[]`, and the symmetric
`customHooks[]` ⟷ `parents[]` linkage.
