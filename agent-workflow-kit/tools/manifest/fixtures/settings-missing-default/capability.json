{
  "family": "agent-workflow",
  "schema": 1,
  "name": "fixture-settings-missing-default",
  "kind": "execution-backend",
  "version": "1.0.0",
  "provides": ["execute"],
  "roles": {
    "execute": { "cmd": "fixture-exec", "source": "bin/run.sh" }
  },
  "settings": [
    { "key": "FIX_FLAG", "kind": "boolean", "appliesTo": ["fixture-exec"], "effect": "an entry without the required default property" }
  ],
  "cost": "subscription",
  "quota": { "kind": "subscription", "finite": true }
}
