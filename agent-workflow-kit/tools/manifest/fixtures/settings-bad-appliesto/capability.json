{
  "family": "agent-workflow",
  "schema": 1,
  "name": "fixture-settings-bad-appliesto",
  "kind": "execution-backend",
  "version": "1.0.0",
  "provides": ["execute"],
  "roles": {
    "execute": { "cmd": "fixture-exec", "source": "bin/run.sh" }
  },
  "settings": [
    { "key": "FIX_FLAG", "kind": "boolean", "default": "0", "appliesTo": ["not-a-declared-cmd"], "effect": "appliesTo names no roles.*.cmd" }
  ],
  "cost": "subscription",
  "quota": { "kind": "subscription", "finite": true }
}
