---
name: fixture-writable-dirs-duplicate
description: A duplicated writableDirs default must fail validation.
metadata:
  version: '1.0.0'
---

# fixture-writable-dirs-duplicate

Test fixture — duplicate `writableDirs` defaults are rejected.
