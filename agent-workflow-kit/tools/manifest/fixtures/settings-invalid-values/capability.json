{
  "family": "agent-workflow",
  "schema": 1,
  "name": "fixture-settings-invalid-values",
  "kind": "execution-backend",
  "version": "1.0.0",
  "provides": ["execute"],
  "roles": {
    "execute": { "cmd": "fixture-exec", "source": "bin/run.sh" }
  },
  "settings": [
    { "key": "FIX_TIER", "kind": "enum", "values": [], "default": null, "appliesTo": ["fixture-exec"], "effect": "an enum with no values" }
  ],
  "cost": "subscription",
  "quota": { "kind": "subscription", "finite": true }
}
