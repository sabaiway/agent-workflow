---
name: fixture-mode-catalog-bad-operands
description: Malformed modeCatalog typed operands[] — must fail validation.
metadata:
  version: '1.0.0'
---

# fixture-mode-catalog-bad-operands

Negative fixture — typed `operands[]`: a unique string `slot` that really appears in the entry's
rendered invocation form, a boolean `required`, and a non-empty `description`.
